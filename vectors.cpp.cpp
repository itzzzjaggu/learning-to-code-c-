#include <iostream>
#include <vector>
using namespace std;

int main(){

    //vector assignment and accessing vector elements...
    // vector <int> vector1;
    // vector <int> vector2;
    // cout<<"The size of the vector1 initially is "<<vector1.size()<<endl;
    
    // vector1.push_back(10);
    // vector1.push_back(20);
    // cout<<"\nThe elements in the vector1 are:"<<endl;
    // cout<<vector1.at(0)<<endl;
    // cout<<vector1.at(1)<<endl;
    // cout<<"The size of the vector1 now is "<<vector1.size()<<endl;
    // cout<<endl;

    // vector2.push_back(100);
    // vector2.push_back(200);
    // cout<<"\nThe elements in the vector2 are:"<<endl;
    // cout<<vector2.at(0)<<endl;
    // cout<<vector2.at(1)<<endl;
    // cout<<"The size of vector2 now is: "<<vector2.size()<<endl;

    // vector <vector<int>> vector_2d ;
    // vector_2d.push_back(vector1);
    // vector_2d.push_back(vector2);

    // cout<<"\nThe elements in the vector 2d are as follows:"<<endl;
    // cout<<vector_2d.at(0).at(0)<<"  "<<vector_2d.at(0).at(1)<<endl;
    // cout<<vector_2d.at(1).at(0)<<"  "<<vector_2d.at(1).at(1)<<endl;;
  

    // vector1.at(0)=1000;
    // cout<<"\nThe elements in the vector 2d now are as follows:"<<endl;
    // cout<<vector_2d.at(0).at(0)<<"  "<<vector_2d.at(0).at(1)<<endl;
    // cout<<vector_2d.at(1).at(0)<<"  "<<vector_2d.at(1).at(1)<<endl;;

    // cout<<"\nThe elements in the vector1 now are:"<<endl;
    // cout<<vector1.at(0)<<endl;
    // cout<<vector1.at(1)<<endl;

    
    


  
    return 0;
}